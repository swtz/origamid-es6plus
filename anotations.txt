// ANOTAÇÕES | ORIGAMID - ES6+ | 05 EFEITOS NO DOM

// AULA 0515 | Local Storage

// -> Session Storage armazena dados enquanto o browser estiver aberto
// -> Local Storage armazena dados na máquina, ou seja, persiste mesmo
// quando o browser é fechado

// -> Cookies é uma outra maneira de salvar dados do usuário

// Nessa aula também foi reforçada a técnica de "ativar uma função pelo nome dela".
// Isso é possível por meio do '[]' | object[<function-name>](arg0, arg1, argN);

// Esses tipos de armazenamento são úteis para preservar dados que
  // (1) não têm necessidade de serem salvos numa base de dados
  // (2) que são úteis para o 'usuário' e não para o 'programador'
 