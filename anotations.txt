// ANOTAÇÕES | ORIGAMID - ES6+ | 05 EFEITOS NO DOM

// AULA 0504 | Tooltip

// A criação do 'tooltip' é dinâmica
// Após a estilização, é necessário obter a posição do cursor em relação à page

// nome do eventListener: 'mouseover' | retorno do event: [object MouseEvent]
// Nesse objeto, existem as props pageX e pageY que indicam a posição do cursor
// em relação à page

// Agora, por meio do javascript, pode-se obter esses valores e
// alinhar o 'tooltip' ao posicionamento do cursor
// HTMLElement.style retorna um objeto onde é possível manipular qualquer
// style do elemento, tais como 'top' e 'left' do 'tooltip'

// nome do eventListener 'mouseleave' | permite disparar uma quando o cursor
// 'deixar' o elemento.

// HTMLElement.addEventListener('event', {}) | suporta um objeto, porém
// esse objeto precisar ter a fn 'handleEvent' que é disparada pelo eventListener;
// é útil para deixar o código mais organizado.

// É uma boa prática remover o eventListener 'mouseleave' ao remover o 'tooltip'

// nome do eventListener: 'mousemove' | esse event é adicionado ao elemento
// que cria o 'tooltip' e o 'callback-object' atualiza o 'top/left' do 'tooltip'
// segundo a posição do cursor em tempo real.

// É uma boa prática remover o eventListener 'mousemove' ao remover o 'tooltip'
