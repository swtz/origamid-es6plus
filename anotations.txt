// ANOTAÇÕES | ORIGAMID - ES6+ | 05 EFEITOS NO DOM

// AULA 0513/0514 | Button Creator

// Abaixo, uma dica de como executar uma função de maneira dinâmica
// com o operador de acesso '[]'
/*
function handleChange(event) {
  const name = event.target.name;
  const value = event.target.value;

  handleStyle[name](value); // #!important
}

const handleStyle = {
  backgroundColor(value) {
    HTMLElement.style.backgroundColor = value;
  },
  height(value) {
    HTMLElement.style.height = `${ value } px`;
  },
  width(value) {
    HTMLElement.style.width = `${ value } px`;
  }
}

HTMLElement.addEventListener('change', handleChange);
*/
