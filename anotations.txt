// ANOTAÇÕES | ORIGAMID - ES6+ | 05 EFEITOS NO DOM

// AULA 0507 | ClickOutside

// Essa aula mostra algumas técnicas de evitar que
// o Event Bubble dispare a callbackFn do evento
// de modo direto. São criadas condicionais para controlar a
// execução dessa callbackFn

// HTMLElement.contains(<HTMLElement>): boolean
// ao chamar a callbackFn (que remove a classe 'ativo' do dropdown-menu)
// deve-se remover o event 'click' para fins de otimização

// é possível adicionar somente 1 evento 'click' no 'html', pois
// esse evento é adicionado toda vez que o user clicar no dropdown-menu

if (!HTMLElement.hasAttribute('data-outside')) {
  html.addEventListener('click', handleOutsideClick);
  element.setAttribute('data-outside', '');
}

// Essa condicional garante que o evento 'click' seja adicionado somente
// 1 vez no 'html'

// É preciso remover esse attr junto com a remoção da classe que ativa o dropdown-menu,
// para que possa ser adicionado o evento 'click' novamente no 'html' (linha 18 do code acima).
